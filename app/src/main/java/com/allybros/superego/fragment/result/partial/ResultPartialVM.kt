package com.allybros.superego.fragment.result.partial

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class ResultPartialVM(application: Application) : AndroidViewModel(application) {
}