package com.allybros.superego.activity.playground

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PlayGroundVM(application: Application) : AndroidViewModel(application) {
}