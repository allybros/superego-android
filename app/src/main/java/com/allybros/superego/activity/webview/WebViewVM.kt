package com.allybros.superego.activity.webview

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class WebViewVM(application: Application) : AndroidViewModel(application) {
}